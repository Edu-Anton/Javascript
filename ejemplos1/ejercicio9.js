/*Write a function that validates whether two strings are identical. Make this validator case */

function match(s1, s2) {
  if(s1.toLowerCase() == s2.toLowerCase()){
    return true;
  }
	return false;
}